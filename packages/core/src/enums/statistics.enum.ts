/**
 * Logical operators for combining conditions
 */
export enum ConditionOperatorEnum {
  AND = 'and',
  OR = 'or',
}

/**
 * States for observables in conditional statistics
 */
export enum ObservableStateEnum {
  ON = 'on',
  OFF = 'off',
}

