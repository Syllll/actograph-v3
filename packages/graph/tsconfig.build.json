{
  /**
   * TypeScript build configuration for @actograph/graph
   * 
   * This config is used for compilation (yarn build).
   * It overrides paths to {} so @actograph/core resolves via node_modules
   * (which points to ../core via file: dependency → uses dist/).
   * 
   * This separation allows:
   * - Development: IDE uses src/ for hot-reload (tsconfig.json)
   * - Build: Compiler uses dist/ for clean output (tsconfig.build.json)
   */
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationMap": true,
    // Override paths to use node_modules resolution → @actograph/core/dist
    "paths": {}
  },
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/__tests__"]
}

