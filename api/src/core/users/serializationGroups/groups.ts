/**
 * Groupes de sérialisation pour class-transformer.
 * Permet de contrôler quels champs sont exposés selon le contexte (utilisateur standard ou admin).
 */

/** Groupe de sérialisation pour les utilisateurs standards */
export const GROUP_USER = 'group_user';

/** Groupe de sérialisation pour les administrateurs */
export const GROUP_ADMIN = 'group_admin';
