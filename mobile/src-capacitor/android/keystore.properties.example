# Release signing configuration for ActoGraph Mobile
#
# 1. Copy this file to keystore.properties:
#      cp keystore.properties.example keystore.properties
#
# 2. Generate a keystore (if you don't have one):
#      keytool -genkey -v \
#        -keystore app/actograph-release.jks \
#        -alias actograph \
#        -keyalg RSA -keysize 2048 \
#        -validity 10000 \
#        -storepass YOUR_STORE_PASSWORD \
#        -keypass YOUR_KEY_PASSWORD \
#        -dname "CN=ActoGraph, OU=Mobile, O=Improba, L=Lyon, ST=Rhone, C=FR"
#
# 3. Fill in the values below:

# Path to the keystore file (relative to android/app/, or absolute)
storeFile=actograph-release.jks
# Password of the keystore
storePassword=
# Alias of the signing key inside the keystore
keyAlias=actograph
# Password of the signing key
keyPassword=
